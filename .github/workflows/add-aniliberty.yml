name: add-aniliberty
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Temporary workflow disabled; AniLiberty integration is being committed directly.'
