name: add-aniliberty
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: write
jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch index.js
        run: python3 .github/workflows/patch_aniliberty.py
      - name: Validate JavaScript
        run: node --check index.js
      - name: Commit integration
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.js
          git diff --cached --quiet && { echo 'No changes made'; exit 1; }
          git commit -m 'Add AniLiberty Russian anime source'
          git push
      - name: Remove temporary integration files
        run: |
          git rm .github/workflows/add-aniliberty.yml .github/workflows/patch_aniliberty.py
          git commit -m 'Remove temporary AniLiberty integration files'
          git push
